<% if signed_in? %>
  <%= current_user.inspect %>
  <%= link_to "Wyloguj", sign_out_path %>
<% end %>
<%= react_component 'AppContainer', props: {
  today: @today_orders_json[:data],
  archived: @not_today_orders_json[:data],
  archived_page_count: @not_today_orders.total_pages,
  current_user: current_user
} %>
